package com.pally.domain.avatar;

/**
 * User-controlled teaching preference per avatar.
 * TEACHING = Socratic guidance (hints, questions to guide the child).
 * DIRECT   = Give the answer directly.
 */
public enum TeachingMode {
    TEACHING,
    DIRECT
}

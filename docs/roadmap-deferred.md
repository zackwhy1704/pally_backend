# Deferred Features (gate: ≥1 committed pilot + closed-book live)

Do NOT start until a real centre has agreed to a pilot AND closed-book
retrieval (centre avatars refusing off-corpus) is confirmed working.

1. Content ingestion → wiki_chunk compile pipeline (web upload tool)
2. Multi-staff centre logins (centre_user auth beyond owner)
3. Assignments (teacher pushes quiz/deck to a class)
